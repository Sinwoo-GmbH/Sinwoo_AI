This folder reserves the workspace route namespace for the shell-internal customer admin view.

The current desktop workspace shell renders this feature inside `PlatformShell` instead of an app-router `page.tsx`.
